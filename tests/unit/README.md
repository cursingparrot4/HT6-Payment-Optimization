# Unit Tests

Pure module tests live below this directory, grouped by project package. Unit tests use fresh small builders, fixed dates, no network, no live server, and no full Sarah dependency unless the loader itself is under test.
