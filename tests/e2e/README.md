# End-to-End Tests

The offline Sarah API workflow and Streamlit smoke tests live here after G4. Keep them deterministic and fixture-provider-backed; browser layout checks remain an explicit manual release step.
