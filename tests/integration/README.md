# Integration Tests

Tests combining adjacent real modules live here: engine plus explanation, API plus engine/data, parser plus API, and eval plus deterministic downstream probes. External providers remain replaced by protocol fakes.
